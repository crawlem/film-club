# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
      
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && AIRT_API_KEY=${{ secrets.AIRT_API_KEY }} AIRT_API_BASE_ID=${{ secrets.AIRT_API_BASE_ID }} TMDB_API_KEY_V4=${{ secrets.TMDB_API_KEY_V4 }} npm run generate
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FILM_CLUB_A2A16 }}'
          channelId: live
          projectId: film-club-a2a16
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
