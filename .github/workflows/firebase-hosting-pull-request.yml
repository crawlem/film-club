# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && AIRT_API_KEY=${{ secrets.AIRT_API_KEY }} AIRT_API_BASE_ID=${{ secrets.AIRT_API_BASE_ID }} TMDB_API_KEY=${{ secrets.TMDB_API_KEY }} npm run generate
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FILM_CLUB_A2A16 }}'
          projectId: film-club-a2a16
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels